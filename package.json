{
  "name": "paint-canvas",
  "author": {
    "name": "SaeMii",
    "email": "wrfg12@gmail.com"
  },
  "homepage": "https://amiru11.github.io/paint-canvas/",
  "scripts": {
    "start": "snowpack dev",
    "build": "rm -rf build && rm -rf docs && snowpack build && mv build docs && touch docs/.nojekyll",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "lint": "prettier --check \"src/**/*.{ts,js}\"",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "canvas-confetti": "^1.2.0"
  },
  "devDependencies": {
    "@snowpack/plugin-typescript": "^1.1.1",
    "@types/canvas-confetti": "^1.0.0",
    "@types/snowpack-env": "^2.3.2",
    "gh-pages": "^3.1.0",
    "prettier": "^2.0.5",
    "snowpack": "^2.18.4",
    "typescript": "^4.0.0"
  }
}
